import React from "react";
import Card from "./Card";

const Screen = ({ text }) => {
  return (
    <div className="screen">
      <h1>{text}</h1>
    </div>
  );
};
export default Screen;
